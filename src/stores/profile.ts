import { writable } from "svelte/store";

export const navbarHeight = writable(0);
export const footerHeight = writable(0);
export const userData = writable(null);
export const languages = writable(null);
